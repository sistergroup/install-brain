# {{VAULT_NAME}}

{{OWNER_NAME}}'s context brain. This folder — `{{VAULT_PATH}}` — is the vault itself and
the only source of truth.

Backup: {{SYNC_PROVIDER}} — {{SYNC_LOCATION}}.

Conventions live in `Meta/Vault Guide.md`: structure, naming, the condensation pipeline
and how the sync works. Read that first; this README deliberately repeats none of it.

Set up {{SETUP_DATE}}.
